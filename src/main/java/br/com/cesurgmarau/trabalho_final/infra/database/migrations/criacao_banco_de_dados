CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    age VARCHAR(10) NOT NULL
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    price FLOAT NOT NULL
);

CREATE TABLE feelings (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE comments (
    id SERIAL PRIMARY KEY,
    description TEXT NOT NULL,
    feeling_id INT NOT NULL,
    product_id INT NOT NULL,
    user_id INT NOT NULL,
    created_at TIMESTAMP DEFAULT now(),

    CONSTRAINT fk_product FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
    CONSTRAINT fk_user FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    CONSTRAINT fk_feeling FOREIGN KEY (feeling_id) REFERENCES feelings(id) ON DELETE SET NULL
);

INSERT INTO feelings (name) VALUES ('Very Positive');
INSERT INTO feelings (name) VALUES ('Positive');
INSERT INTO feelings (name) VALUES ('Neutral');
INSERT INTO feelings (name) VALUES ('Negative');
INSERT INTO feelings (name) VALUES ('Very Negative');

